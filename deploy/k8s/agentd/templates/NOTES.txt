{{- if .Values.database.dsn }}
agentd and all Agentlets use the configured external MySQL database.
{{- else }}
agentd and all Agentlets share the built-in MySQL Deployment and its PVC.
{{- end }}

Health endpoint:

  kubectl -n {{ .Release.Namespace }} port-forward service/{{ include "agentd.fullname" . }} 8020:{{ .Values.service.port }}
  curl http://127.0.0.1:8020/healthz
